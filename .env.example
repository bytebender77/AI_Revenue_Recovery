# cp .env.example .env and fill in. Never commit .env.
# Do NOT quote the value -- make include would keep the quotes.
OPENAI_API_KEY=sk-proj-...
# Optional: override if your account does not serve the default model.
# RR_OPENAI_MODEL=gpt-4o-2024-08-06
# ANTHROPIC_API_KEY=sk-ant-...
